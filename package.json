{
  "name": "beefcake",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "server:dev": "wrangler dev --local",
    "server:types": "wrangler types server/worker-configuration.d.ts",
    "server:check": "wrangler deploy --dry-run --outdir server/dist",
    "server:deploy": "wrangler deploy"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "chartjs-adapter-date-fns": "^3.0.0",
    "date-fns": "^4.4.0",
    "idb": "^8.0.3",
    "jose": "^6.2.8",
    "preact": "^10.29.7",
    "wouter": "^3.10.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^24.13.3",
    "esbuild": "^0.28.1",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
